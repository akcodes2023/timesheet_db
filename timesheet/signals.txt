from django.dispatch import Signal
# from django.dispatch import receiver
# from .views import get_timesheets_by_date_range
# from .views import get_timesheet

date_range_selected = Signal()


'''
# Emit the signal when the user selects a date range
start_date = "2023-01-01"
end_date = "2023-12-31"


@receiver(date_range_selected)
def handle_date_range_selected(sender, **kwargs):
    start_date = kwargs.get('start_date')
    end_date = kwargs.get('end_date')

    # Use the reusable function to get timesheets by date range
    # get_timesheets_by_date_range(start_date, end_date)
    get_timesheet(request=None, start_date=start_date, end_date=end_date)


date_range_selected.send(sender=None, start_date=start_date, end_date=end_date)
'''
